public class GameState {

    boolean won;
    boolean lost;
    boolean inProgress;
    Board board;
/*
    public boolean isWon() {
        if (board.bombCounter ==0 && board.markedFields==board.size);
        return won;

    }

    public boolean isLost() {
        return lost;
    }

    public boolean isInProgress() {
        return inProgress;
    }
    */
}
